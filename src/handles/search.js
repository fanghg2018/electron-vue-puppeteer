const puppeteer = require('puppeteer')
const fs = require('fs')
const request = require('request')
const path = require('path')
const keepUri = process.env.NODE_ENV === 'development' ? '../../../../../' : '../../'
const searchCralwer = async (url, win) => {

}
export default searchCralwer
